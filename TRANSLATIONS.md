# 雙語維護指南 (i18n)

本站採「**英文為原文 + 執行期 JavaScript 切換中文**」的單檔架構。
你平常**只需編輯英文版 `index.html` 一個檔案**,不再有獨立的中文檔要同步。

---

## 如何新增／修改中文翻譯

翻譯對照表就在 `index.html` 底部的 i18n `<script>` 裡的 `ZH` 物件。

1. **照常在 HTML 寫英文內容**(標題、段落、按鈕等)。
2. 到 `ZH` 物件裡加**一行**:

   ```js
   var ZH = {
     ...
     "英文原文(要和 HTML 完全一致)": "中文譯文",
   };
   ```

例如新增一個服務卡片標題 `<h3>New Service</h3>`,就加:

```js
"New Service": "新服務項目",
```

完成。切到中文時會自動替換。

### 規則
- **鍵值(英文)必須和畫面上文字完全相同**(前後空白會自動忽略,但中間文字要一字不差)。
- 若某句**中英文相同**(例如 `Creo`、`DFM`),**不用加**——沒對照就維持原文。
- 相同英文字串在多處出現時,只需在 `ZH` 加一次,全站套用。

### 可翻譯的範圍
文字內容、以及這些屬性:`title`、`alt`、`placeholder`、`data-title`、`<meta content>`、`<title>`。
若要翻譯的字**寫在 `<script>` 裡**(像複製成功訊息),用 `window.__t('英文')` 取得對應語言字串,並記得把該字串也加進 `ZH`。

---

## 語言是怎麼決定的
優先序:網址 `?lang=` 參數 → `localStorage` 記住的選擇 → 瀏覽器語言 → 預設英文。
- `/` 或 `/?lang=en` → 英文
- `/?lang=zh` → 中文
- header 的「中文 / EN」鈕:原地切換,不換頁。

## 舊網址
`zh/index.html` 已改為**自動導向** `/?lang=zh`,舊書籤與搜尋結果不會失效。請勿刪除此檔。

## SEO
`index.html` 的 `<head>` 內有 `hreflang` 標籤,`sitemap.xml` 的中文條目指向 `/?lang=zh`。
新增分頁時,若也要雙語,沿用同一套 `?lang=` 慣例即可。
